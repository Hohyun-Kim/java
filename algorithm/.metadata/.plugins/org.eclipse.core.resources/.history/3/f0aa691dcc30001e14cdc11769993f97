package com.ssafy.dice;

import java.util.Scanner;

public class DiceTest {
	
	static int N, numbers[] ;
	public static void main(String[] args) {
		
		Scanner sc = new Scanner(System.in);
		N = sc.nextInt();
		int M = sc.nextInt();
		numbers = new int[N];
		switch (M) {
		case 1: // 중복순열
			break;
		case 2: // 순열
			break;
		}
	}
	
	private static void dice2(int cnt)	{ // n번째 주사위 던지기
		
	}

}
